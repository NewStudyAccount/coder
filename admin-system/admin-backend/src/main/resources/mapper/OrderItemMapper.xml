<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.mapper.OrderItemMapper">

    <resultMap id="OrderItemResult" type="com.admin.entity.OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="skuId" column="sku_id"/>
        <result property="productName" column="product_name"/>
        <result property="skuName" column="sku_name"/>
        <result property="image" column="image"/>
        <result property="price" column="price"/>
        <result property="quantity" column="quantity"/>
        <result property="totalPrice" column="total_price"/>
    </resultMap>

    <select id="selectByOrderId" resultMap="OrderItemResult">
        SELECT * FROM mall_order_item WHERE order_id = #{orderId}
    </select>

    <insert id="batchInsert">
        INSERT INTO mall_order_item (order_id, product_id, sku_id, product_name, sku_name, image, price, quantity, total_price)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.skuId}, #{item.productName}, #{item.skuName}, #{item.image}, #{item.price}, #{item.quantity}, #{item.totalPrice})
        </foreach>
    </insert>
</mapper>

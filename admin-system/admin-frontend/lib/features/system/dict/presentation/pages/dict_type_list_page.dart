import 'package:flutter/material.dart';

class DictTypeListPage extends StatelessWidget {
  const DictTypeListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('字典管理', style: Theme.of(context).textTheme.headlineSmall));
  }
}

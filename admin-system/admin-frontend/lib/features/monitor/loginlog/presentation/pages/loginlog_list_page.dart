import 'package:flutter/material.dart';

class LoginLogListPage extends StatelessWidget {
  const LoginLogListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('登录日志', style: Theme.of(context).textTheme.headlineSmall));
  }
}
